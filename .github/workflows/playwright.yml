name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '25'
      - name: Build & Install
        run: mvn -B install -D skipTests --no-transfer-progress
      - name: Ensure browsers are installed
<<<<<<< HEAD
        run: mvn exec:java -e -D exec.mainClass=com.microsoft.playwright.CLI -D exec.args="install --with-deps"
      - name: Run tests
        run: mvn clean test -PFramework -Dbrowser=chrome -Denv=qa -Dtimeout=1200

      - name: Run tests
        run: mvn clean test -PFramework -Dbrowser=chrome -Denv=qa -Dtimeout=1200
=======
        run: mvn exec:java -e -D exec.mainClass=com.microsoft.playwright.CLI -D exec.args="install --with-deps
     -  name: Run tests
       run: mvn clean test -PFramework -Dbrowser=chrome -Denv=qa -Dtimeout=120
  #    - name: Run tests
   #     run: mvn clean test -PRegression -Dbrowser=chrome -Denv=qa
>>>>>>> 8e7a38c9f273e35cf5a09f08613b602848719fad
